# 测试用 SDK 默认商户配置。
# 单元测试通过该文件验证配置加载、JWT 签名、请求加密和响应解密链路。
# 这里的密钥只服务本地测试，不能作为生产配置样例直接复制使用。
payment.gateway.base-url=https://payment-gateway.example.com
payment.gateway.merchant-no=200046
payment.gateway.livemode=false
payment.gateway.api-private-key=sdk-test-jwt-secret-change-me-32-bytes-only
payment.gateway.platform-request-public-key-path=classpath:keys/2606177036_platform_request_publicKey.pem
payment.gateway.merchant-response-private-key-path=classpath:keys/2606177036_merchant_response_privateKey.pem
payment.gateway.connect-timeout-ms=5000
payment.gateway.read-timeout-ms=10000
